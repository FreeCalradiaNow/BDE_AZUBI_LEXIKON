<Window x:Class="BDE_AZUBI_LEXIKON.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BDE_AZUBI_LEXIKON"
        mc:Ignorable="d"
        Title="MainViewModel" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>        
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" Text="{Binding PlatzhalterA}" Margin="175,50,0,0" Height="70" Width="150" Grid.Column="1"/>
        <TextBox HorizontalAlignment="Left" Margin="175,134,0,0" Text="{Binding PlatzhalterB}" TextWrapping="Wrap" VerticalAlignment="Top" Height="70" Width="150" Grid.Column="1"/>
        <TextBlock HorizontalAlignment="Left" Margin="175,255,0,0" Text="{Binding PlatzhalterAB}" TextWrapping="Wrap" VerticalAlignment="Top" Height="70" Width="150" Grid.Column="1"/>
    </Grid>
</Window>
